<% if current_user.nil? %>
  <p><%= link_to "Sign in with Facebook", user_omniauth_authorize_path(:facebook) %></p>
<% else %>
  <p>Signed in as <%= current_user.name %>.  <%= link_to "Sign out", destroy_user_session_path, :method => :delete %></p>
<% end %>
